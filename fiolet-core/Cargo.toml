[package]
name = "fiolet_core"
version = "0.1.0"
edition = "2021"
authors = ["Adrian Maliszewski <maliszewskiadrian01@gmail.com>"]
description = "Deterministic pre-semantic safety kernel (ATOMIC HALT)"
license = "MIT"
repository = "https://github.com/maliszewskiadrian/FINAL_FIOLET_ENGINE"
readme = "../README.md"

[lib]
name = "fiolet_core"
path = "src/lib.rs"
crate-type = ["lib", "staticlib", "cdylib"]

# ==================================================
# FEATURES
# ==================================================

[features]
default = ["kernel"]
kernel = []        # pure no_std safety kernel
std = []           # reserved ONLY for host-side tooling (NOT kernel)

# ==================================================
# PROFILES — THIS FIXES YOUR ERROR
# ==================================================

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[profile.test]
panic = "abort"

[profile.bench]
panic = "abort"
